const a = require("debug")("worker:a"),
  b = require("debug")("worker:b"),
  c = require("debug")("worker:c"),
  d = require("debug")("worker:d");

async function work(worker, description) {
  let worker_a = a;
  let worker_b = b;
  let worker_c = c;

  worker = async(description) = {
   
  };
}
